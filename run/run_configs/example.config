#location of RV and CV gene list files
datadir=/path/to/input/genelists/ 
#location to output results
outdir=/path/to/output/results/
#List of RV gene list files
trait_listR=$datadir/rare.traitlist
# Matched list of CV gene list files
trait_listC=$datadir/common.traitlist
# q=1 to perform quantitative NetColoc
q=1
# How gene scores should be transformed. Only neglog10 currently supported
transform=neglog10
# How scores should be normalized: minmax, sum or max
normalization=sum
# Additional thresholding of inputs
th=None
# How to handle genes identified by both RV and CV. bin to permute separately, or None to do nothing. 
overlap=bin

